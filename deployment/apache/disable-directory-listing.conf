# Disable Apache-generated directory indexes for every served path under /var/www.
# Applications and files remain accessible through their explicit URLs.
<DirectoryMatch "^/var/www(?:/|$)">
    Options -Indexes
</DirectoryMatch>
